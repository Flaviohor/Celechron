name: pcelechron
description: Time scheduler for zjuers.
publish_to: "none"
version: 1.3.3+1
environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  get:
  cupertino_icons:
  table_calendar:
  hive:
  hive_flutter:
  uuid:
  flutter_svg:
  intl:
  extended_sliver:
  flutter_launcher_icons:
  rename_app:
  url_launcher:
  quiver: ^3.2.1
  # 作者 fork 的 workmanager。上游用 `ref: main`（浮动分支），每次 pub get 都要
  # 联网 fetch 一次 GitHub 才能把 main 解析成具体 commit。
  # 这里改成钉死 commit，pub 会直接读本地缓存、跳过 fetch —— 国内网络下 GitHub
  # 不可达时也能正常解析依赖。升级该依赖时手动改这个 SHA 即可。
  workmanager:
    git:
      url: https://github.com/Celechron/flutter_workmanager.git
      ref: d7e6ba5ef3796fafe54d9739e481f01058751b4d
      path: workmanager
  # 17.x 不支持 Windows；Windows（C++/WinRT Toast）自 19.0.0 起提供。
  flutter_local_notifications: ^22.0.0
  flutter_secure_storage: ^9.2.4
  qr_flutter:
  app_links:
  crypto: 
  path_provider:
  share_plus: ^12.0.1 # 9fb0fb 提交当中提升了版本
  device_calendar:
  timezone:
  device_info_plus:

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  pigeon: any

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/

  fonts:
    - family: NotoSansSC
      fonts:
        - asset: assets/fonts/NotoSansSC-VF.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

flutter_launcher_icons:
  image_path: assets/logo.png
  android: true
  ios: true
  remove_alpha_ios: true
